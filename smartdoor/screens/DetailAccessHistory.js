import * as React from "react";
import { useState } from "react";
import {
  Image,
  StyleSheet,
  Text,
  View,
  TouchableOpacity,
  ScrollView,
} from "react-native";

const DetailAccessHistory = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Detail!</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
});

export default DetailAccessHistory;
